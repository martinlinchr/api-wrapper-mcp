services:
  api-wrapper-mcp:
    build: .
    container_name: api-wrapper-mcp
    environment:
      API_GATEWAY_TOKEN: "super-secret-mcp-token"
      RYBBIT_API_KEY: "${RYBBIT_API_KEY}"
    ports:
      - "3000:3000"
